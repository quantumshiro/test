#include <iostream>

int main()
{
    int a, b, c, d, e, f;
    while ((std::cin >> a >> b >> c >> d >> e >> f) != EOF)
    {
        double y = (double) (c*d - a*f) / (double) (b*d - a*e);

        if (a != 0) cout << 
    }
}